name: Mobile-Number-Issuer-Detector-Deployment
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  print_hello:
    runs-on: ubuntu_latest
    steps:
      - run: echo "Hello Detector"